{
  "name": "@leafypay/giam-client",
  "version": "1.0.0",
  "private": true,
  "description": "The verifier every resource server uses against the identity authority. One implementation, so four cannot disagree about which forgeries to refuse.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.9.3"
  }
}
