{
  "name": "@leafypay/mongo-compat",
  "version": "3.1.4",
  "description": "Which MongoDB this deployment targets (Atlas or Enterprise Advanced, and which version) and what it accepts for Queryable Encryption. Shared by the PSP and bankcore so the compatibility table exists once.",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.9.3"
  }
}
