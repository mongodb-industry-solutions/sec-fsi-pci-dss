{
  "name": "@leafypay/eventbus",
  "version": "1.0.0",
  "description": "Shared domain event bus (in-process, Kafka, RabbitMQ) with a MongoDB event store. Used by the PSP backend and by bankcore.",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "uuid": "^14.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.9.3"
  }
}
