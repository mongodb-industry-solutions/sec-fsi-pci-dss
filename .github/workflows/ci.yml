name: CI

on:
  push:
    branches: [develop, staging, giam]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # The GIAM API document is a deliverable, so it is checked mechanically rather than in review.
  # Three failures this job catches that a reviewer would not: a route that exists and is not in the
  # document, a document committed from a source tree it no longer matches, and an operation missing
  # the error cases an integrator has to handle.
  giam-contract:
    name: GIAM API contract
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install workspace packages
        run: |
          npm install --no-audit --no-fund
          npm install --prefix packages/eventbus --no-audit --no-fund
          npm install --prefix packages/platform-links --no-audit --no-fund
          npm install --prefix giam/backend --no-audit --no-fund

      - name: Type-check
        run: npm run type-check --prefix giam/backend

      - name: Re-emit the OpenAPI document
        run: npm run openapi:emit --prefix giam/backend

      # The committed document must equal the one the current source produces. Without this the file
      # is a snapshot of whatever the author last remembered to regenerate.
      - name: The committed document matches the source
        run: |
          if ! git diff --exit-code -- giam/backend/openapi.json; then
            echo "::error::giam/backend/openapi.json is stale. Run: npm run openapi:emit --prefix giam/backend"
            exit 1
          fi

      - name: Spec-lint
        run: npm run openapi:lint --prefix giam/backend

      # Route coverage, the "names a specification or says none applies" rule, and the
      # provably-public rule. None of them are expressible as a linter rule.
      - name: Contract tests
        run: npx vitest run test/giam/backend/unit

  # GIAM's own suite. Deliberately scoped to GIAM: between the extraction phases the other
  # applications are expected to be broken, and a job that fails on them would stop reporting
  # anything useful about the work actually in progress.
  giam-tests:
    name: GIAM unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install workspace packages
        run: |
          npm install --no-audit --no-fund
          npm install --prefix packages/eventbus --no-audit --no-fund
          npm install --prefix packages/platform-links --no-audit --no-fund
          npm install --prefix giam/backend --no-audit --no-fund

      - name: Unit tests
        run: npx vitest run test/giam/backend/unit
