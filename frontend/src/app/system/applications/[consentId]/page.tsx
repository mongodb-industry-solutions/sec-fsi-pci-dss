// v18: canonical authorized-app detail (scopes, approval date/time, revoke, operations).
// Re-exports the profile detail view (DRY, self-scoped).
export { default } from '../../profile/applications/[consentId]/page';
