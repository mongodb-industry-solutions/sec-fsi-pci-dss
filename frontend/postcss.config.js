// Tailwind CSS v4 moves the PostCSS plugin to a dedicated package and bundles
// autoprefixer, so no separate autoprefixer entry is needed.
module.exports = { plugins: { '@tailwindcss/postcss': {} } };
